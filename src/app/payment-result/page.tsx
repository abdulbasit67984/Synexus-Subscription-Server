export default async function Result({searchParams}:{searchParams:Promise<{orderId?:string}>}){const {orderId}=await searchParams;return <main className="shell"><section className="card" style={{maxWidth:560,margin:"60px auto"}}><h1>Payment received for processing</h1><p className="muted">Order {orderId||'unknown'}</p><p>Return to PANDAS ERP and select “Check payment status”. Access is restored only after EasyPaisa confirms the payment.</p></section></main>}
